defaults:
  - default.yaml
  
extra_params:
  data_prep:
    exp_suffix: seen7_context-${data.dataset.config.add_context}
    seen_airports: [kbos, kewr, ksea, kmdw, ksfo, kdca, panc]
    unseen_airports: [kmsy, kjfk, klax]