defaults:
  - default.yaml

dataset:
  config:
    sampling_strategy: critical

extra_params:
  data_prep:
    exp_suffix: seen2_context-${data.dataset.config.add_context}
    seen_airports: [kewr, kmdw]
    unseen_airports: [kdca, ksea, kmsy, kjfk, ksfo, panc, kbos, klax]